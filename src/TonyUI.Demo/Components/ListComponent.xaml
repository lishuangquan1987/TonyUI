<UserControl x:Class="TonyUI.Demo.Components.ListComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TonyUI.Demo.Components"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Margin="20">
            <!-- ListBox -->
            <TextBlock Text="ListBox" 
                       Style="{StaticResource Heading3Style}" 
                       Margin="0,0,0,16" />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,32">
                <ListBox Width="200" Height="200" Margin="0,0,16,0">
                    <ListBoxItem Content="Item 1" />
                    <ListBoxItem Content="Item 2" />
                    <ListBoxItem Content="Item 3" />
                    <ListBoxItem Content="Item 4" />
                    <ListBoxItem Content="Item 5" />
                </ListBox>
                
                <ListBox Width="200" Height="200" IsEnabled="False">
                    <ListBoxItem Content="Disabled Item 1" />
                    <ListBoxItem Content="Disabled Item 2" />
                    <ListBoxItem Content="Disabled Item 3" />
                </ListBox>
            </StackPanel>

            <!-- ListView -->
            <TextBlock Text="ListView" 
                       Style="{StaticResource Heading3Style}" 
                       Margin="0,0,0,16" />
            <ListView Width="400" Height="200" Margin="0,0,0,32">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Age" Width="80" DisplayMemberBinding="{Binding Age}" />
                        <GridViewColumn Header="City" Width="120" DisplayMemberBinding="{Binding City}" />
                    </GridView>
                </ListView.View>
                <ListViewItem>
                    <GridViewRowPresenter Content="John Doe,25,New York" />
                </ListViewItem>
                <ListViewItem>
                    <GridViewRowPresenter Content="Jane Smith,30,Los Angeles" />
                </ListViewItem>
                <ListViewItem>
                    <GridViewRowPresenter Content="Bob Johnson,45,Chicago" />
                </ListViewItem>
            </ListView>

            <!-- TreeView -->
            <TextBlock Text="TreeView" 
                       Style="{StaticResource Heading3Style}" 
                       Margin="0,0,0,16" />
            <TreeView Width="300" Height="200">
                <TreeViewItem Header="Root" IsExpanded="True">
                    <TreeViewItem Header="Child 1" />
                    <TreeViewItem Header="Child 2">
                        <TreeViewItem Header="Grandchild 1" />
                        <TreeViewItem Header="Grandchild 2" />
                    </TreeViewItem>
                    <TreeViewItem Header="Child 3" />
                </TreeViewItem>
            </TreeView>
        </StackPanel>
    </ScrollViewer>
</UserControl>